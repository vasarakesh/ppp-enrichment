# Runtime dependencies for ppp-enrichment (see pyproject.toml and src/ppp_enrichment imports).
# Install: pip install -r requirements.txt

pandas>=2.0.0,<3
httpx>=0.25.0,<1
beautifulsoup4>=4.12.0,<5
lxml>=5.2.0,<6
python-dotenv>=1.0.0,<2
tenacity>=8.2.0,<10
ddgs>=6.0.0,<10
